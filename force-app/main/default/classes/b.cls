public class b {
    public static void m(){
    list<list<sobject>> s = [find 'raju' in sidebar fields returning account(Name),contact(Firstname,lastname)];
    //for(Sobject sk : s)
	system.debug(s);
    }
}